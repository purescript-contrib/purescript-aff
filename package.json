{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "purescript": "^0.9.1-rc.1",
    "pulp": "^9.0.0",
    "rimraf": "^2.5.2"
  }
}
